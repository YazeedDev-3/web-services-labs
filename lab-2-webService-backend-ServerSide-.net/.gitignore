## Build output
bin/
obj/

## Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

## NuGet
*.nupkg
packages/
project.lock.json
project.fragment.lock.json
artifacts/

## OS
Thumbs.db
.DS_Store

## Logs
*.log

## Database files — keep the project .mdf/.ldf so the app runs out of the box after cloning
# (The standard VS .gitignore excludes *.mdf and *.ldf; we override that here intentionally)
!**/*.mdf
!**/*.ldf
